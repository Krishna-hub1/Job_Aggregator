import {useEffect,useState} from 'react'; import {Building2,Search,ExternalLink} from 'lucide-react'; import {Link} from 'react-router-dom'; import {api} from '../services/api'; import CompanyLogo from '../components/CompanyLogo';
export default function Companies(){const[companies,setCompanies]=useState([]);const[q,setQ]=useState('');const[loading,setLoading]=useState(true);const load=()=>api.getCompanies(q).then(r=>setCompanies(r.data)).finally(()=>setLoading(false));useEffect(()=>{const t=setTimeout(load,250);return()=>clearTimeout(t)},[q]);return <div className="simple-page"><div className="page-heading"><div><p>EMPLOYERS</p><h1>Companies</h1><span>Explore companies and their currently active roles.</span></div></div><div className="company-search"><Search size={18}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search companies..."/></div>{loading?<div className="page-card">Loading companies...</div>:<div className="company-grid">{companies.map(c=><article className="company-card" key={c.id}><CompanyLogo company={c}/><div><h3>{c.name}</h3><p>{c.job_count} active jobs</p><small>{c.ats_platform||'Job source'}</small></div><div className="company-links">{c.website&&<a href={c.website} target="_blank" rel="noreferrer"><ExternalLink size={16}/></a>}<Link to={`/companies/${c.id}`}>View jobs</Link></div></article>)}{!companies.length&&<div className="page-card"><Building2 size={38}/><h3>No companies found</h3></div>}</div>}</div>}
